<%- include('partials/head') %> <%- include('partials/navbar') %> The random
number is: <%= rand %> <% if (rand % 2 === 0) { %>
<h2>That is an even number!</h2>
<% } else { %>
<h2>That is an odd number!</h2>
<% } %>
<h3><%= rand % 2 === 0 ? 'EVEN' : 'ODD' %></h3>
<%- include('partials/footer') %>
